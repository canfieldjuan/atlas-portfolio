# Dependencies (root node_modules is a symlink to web/node_modules — no trailing slash so it matches)
node_modules

# Next.js build output
.next/
out/

# Vercel
.vercel

# Python audit-script byproduct (scripts/*.py)
__pycache__/
*.pyc
